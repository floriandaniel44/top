# Example environment variables for local development and deployment
# Copy this file to `.env` and fill in the real values (do NOT commit the real .env)

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# Server-side secrets (do NOT expose these to the frontend)
# If you have serverless functions or backend services, set these only on the server.
# SUPABASE_SERVICE_ROLE_KEY=
# RESEND_API_KEY=
