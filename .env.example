# Example environment variables for local development
# Copy this file to `.env` and fill the real values. Do NOT commit your `.env` file.

# Supabase (frontend / public values only)
VITE_SUPABASE_PROJECT_ID=pfaxhagxhwgcdzaaueha
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key-here
VITE_SUPABASE_URL=https://pfaxhagxhwgcdzaaueha.supabase.co

# Notes:
# - Keep secrets (SERVICE_ROLE_KEY, RESEND_API_KEY, ...) in Supabase secrets or other secret manager.
# - Remove any quotes when you fill the values (or keep them if your workflow expects them).
